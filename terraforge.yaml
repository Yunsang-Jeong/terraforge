provider:
  - name: aws
    region: ap-northeast-2
    assume_role:
      role_arn: "1"
      session_name: "2"

variable:
  - name : test
    type: string
    description: this is test
    default: test
